# What the container needs to SERVE a prediction.
#
# No `-r requirements.txt`: that pulled matplotlib, scikit-learn and pandas into
# a serving image while this file's comment claimed they were absent.
#
# No tensorflow and no keras either. Serving runs the ONNX export, which loads
# the same graph in ~67MB against TensorFlow's ~1GB -- the difference between
# fitting a 512MB free tier and not. Training still uses Keras; serving does not.
numpy
opencv-python-headless
onnxruntime

mediapipe==0.10.9

fastapi
uvicorn[standard]
python-multipart

# No gradio. It cost 1.6s of import time and ~150MB resident, measured, and on a
# free plan that spins down after 15 minutes idle both are paid by whoever clicks
# the link next. The deployed UI is a static page served by FastAPI instead.
# scripts/demo.py still uses gradio for local work; nothing deployed does.
httpx
