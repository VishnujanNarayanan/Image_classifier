# What a hosted demo needs to SERVE a prediction. Training-only packages
# (keras-tuner, matplotlib, scikit-learn, pandas) are deliberately absent.
#
# Serving runs the ONNX export rather than Keras: ~67MB resident instead of
# ~1GB, which is what makes a free tier viable at all.
numpy
opencv-python-headless
onnxruntime
httpx

mediapipe==0.10.9

# Must match `sdk_version` in space/README.md. Not 4.44.0: that release imports
# HfFolder from huggingface_hub, removed in huggingface-hub 1.x, so it fails at
# import against current transitive dependencies.
gradio==6.26.0
